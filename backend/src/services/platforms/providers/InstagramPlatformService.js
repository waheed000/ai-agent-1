/**
 * InstagramPlatformService
 * Stub — to be implemented in a future phase.
 * Extends BasePlatformService to enforce the full provider interface.
 */

import { BasePlatformService } from '../BasePlatformService.js';

export class InstagramPlatformService extends BasePlatformService {
  constructor(platform, config) {
    super(platform, config);
  }
  // All methods inherited from BasePlatformService throw "not implemented".
  // Replace each with a real implementation when this provider is built.
}

export default InstagramPlatformService;
