/**
 * XPlatformService (formerly Twitter)
 * Stub — to be implemented in a future phase.
 */

import { BasePlatformService } from '../BasePlatformService.js';

export class XPlatformService extends BasePlatformService {
  constructor(platform, config) {
    super(platform, config);
  }
}

export default XPlatformService;
